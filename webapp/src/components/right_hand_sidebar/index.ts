import { connect } from 'react-redux'
import RightHandSideView from './RightHandSideView';

const mapState2Props = (state: any) => {
  return {
  };
}

export default connect(mapState2Props)(RightHandSideView);
